; Per-project PHP upload limits - PHP-FPM reads .user.ini from the script's
; directory, so this affects only this site, never the server-wide php.ini.
; Picked up automatically within 5 min (user_ini.cache_ttl) of deploy.
upload_max_filesize = 10M
post_max_size = 110M
